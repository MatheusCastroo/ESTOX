-- Create users table for PHP authentication
-- This replaces Supabase Auth
-- MySQL Version

-- Nota: A tabela de usuários já foi criada no script 001-create-tables.sql
-- Este arquivo é mantido apenas para compatibilidade e referência.
-- Se a tabela já existe, nenhuma ação será tomada.

-- A tabela users já está incluída no script principal (001-create-tables.sql)
-- com todas as configurações necessárias para MySQL.
